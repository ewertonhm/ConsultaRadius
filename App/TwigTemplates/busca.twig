{% extends "base.twig" %}

{% block content %}
    <form method="get" action="busca.php">
        <fieldset>
            <label for="nameField">Buscar</label>
            <input type="text" name="buscar" {% if buscar %}value="{{ buscar }}"{% else %}placeholder="Nome, PPPoE ou CPF/CNPJ" {% endif %}>
            <input class="button-primary" type="submit" value="Buscar">
        </fieldset>
    </form>
    {% if clientes %}
        <table>
            <thead>
            <tr>
                <th>Cliente</th>
                <th>PPPoE</th>
                <th>Endereço</th>
                <th>Status</th>
            </tr>
            </thead>
            <tbody>
                {% for cliente in clientes %}
                    <tr>
                        <td><a href="index.php?id={{ cliente.id }}">{{ cliente.nome }} - {{ cliente.documento }}</a></td>
                        <td><a href="index.php?id={{ cliente.id }}">{{ cliente.pppoe }} - {{ cliente.servico }}</a></td>
                        <td><a href="index.php?id={{ cliente.id }}">{{ cliente.endereco }} - {{ cliente.cidade }}</a></td>
                        <td><a href="index.php?id={{ cliente.id }}">{{ cliente.stcontrato }}</a></td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% endif %}
{% endblock %}